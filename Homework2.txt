using System;
using System.Collections.Generic;

class Program
{

/* 2.1
    static List<int> FindMiddleElements(List<int> numbers)
    {
        List<int> middleElements = new List<int>();

        if (numbers.Count % 2 == 1)
        {
            int middleIndex = numbers.Count / 2;
            middleElements.Add(numbers[middleIndex]);
        }
        else
        {
            int middleIndex1 = numbers.Count / 2 - 1;
            int middleIndex2 = numbers.Count / 2;
            middleElements.Add(numbers[middleIndex1]);
            middleElements.Add(numbers[middleIndex2]);
        }

        return middleElements;
    }

    static void Main(string[] args)
    {
        List<int> numbers = new List<int> { 1, 2, 3, 4, 5 };
        List<int> middleElements = FindMiddleElements(numbers);

        Console.WriteLine("Middle element(s):");
        foreach (int num in middleElements)
        {
            Console.WriteLine(num);
        }
    }
*/



/* 2.2
static int FindLargest(List<int> numbers)
    {
        int largest = numbers[0];

        foreach (int num in numbers)
        {
            if (num > largest)
                largest = num;
        }

        return largest;
    }

    static void Main(string[] args)
    {
        List<int> numbers = new List<int> { 1, 5, 3, 9, 2 };
        int largest = FindLargest(numbers);

        Console.WriteLine("Largest integer: " + largest);
    }
*/





}
